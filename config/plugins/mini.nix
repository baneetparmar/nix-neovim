{ ... }:
{
  plugins.mini.enable = true;

  plugins.mini.modules = {
    animate = { };
    bracketed = { };
    clue = { };
    comment = { };
    completion = { };
    diff = { };
    files = { };
    fuzzy = { };
    git = { };
    hipatterns = { };
    icons = { };
    indentscope = { };
    move = { };
    notify = { };
    pairs = { };
    pick = { };
    starter = { };
    statusline = { };
    surround = { };
    test = { };
    extra = { };
  };
}
